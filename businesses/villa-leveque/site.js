document.querySelectorAll('[data-menu]').forEach(button=>button.addEventListener('click',()=>document.querySelector('.nav-links')?.classList.toggle('open')));document.querySelectorAll('form[data-static-form]').forEach(form=>form.addEventListener('submit',event=>{event.preventDefault();form.querySelector('.form-note').textContent='Thank you — this static preview is ready for a form endpoint before launch.';}));
