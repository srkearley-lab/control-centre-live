const header=document.querySelector('.site-header');const nav=document.querySelector('.nav-links');
document.querySelectorAll('[data-menu]').forEach(b=>b.addEventListener('click',()=>nav?.classList.toggle('open')));
addEventListener('scroll',()=>header?.classList.toggle('scrolled',scrollY>28),{passive:true});
const current=location.pathname.split('/').pop()||'index.html';document.querySelectorAll('.nav-links a').forEach(a=>{if(a.getAttribute('href')===current)a.classList.add('active');a.addEventListener('click',()=>nav?.classList.remove('open'));});
const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;let lenis;
if(window.Lenis&&!reduce&&innerWidth>760){lenis=new Lenis({lerp:.075,smoothWheel:true});lenis.on('scroll',()=>window.ScrollTrigger&&ScrollTrigger.update());function raf(t){lenis.raf(t);requestAnimationFrame(raf)}requestAnimationFrame(raf)}
if(window.gsap&&!reduce){gsap.registerPlugin(ScrollTrigger);gsap.set('.reveal,.image-mask',{autoAlpha:0,y:42});gsap.utils.toArray('.reveal,.image-mask').forEach(el=>gsap.to(el,{autoAlpha:1,y:0,duration:1.05,ease:'power3.out',scrollTrigger:{trigger:el,start:'top 84%'}}));gsap.utils.toArray('.image-mask img').forEach(img=>gsap.fromTo(img,{scale:1.14},{scale:1,duration:1.35,ease:'power3.out',scrollTrigger:{trigger:img,start:'top 86%'}}));gsap.utils.toArray('.hero-copy>*').forEach((el,i)=>gsap.from(el,{autoAlpha:0,y:42,duration:1.1,delay:.16+i*.11,ease:'power3.out'}));gsap.utils.toArray('.hero-media img').forEach(img=>gsap.to(img,{yPercent:8,ease:'none',scrollTrigger:{trigger:img.closest('section'),start:'top top',end:'bottom top',scrub:true}}));document.querySelectorAll('.motion-rail').forEach(sec=>{const track=sec.querySelector('.rail-track');if(track&&innerWidth>760){gsap.to(track,{x:()=>-(track.scrollWidth-innerWidth+innerWidth*.12),ease:'none',scrollTrigger:{trigger:sec,start:'top top',end:()=>'+='+(track.scrollWidth*.78),scrub:1,pin:true,anticipatePin:1}})}});if(innerWidth>980){document.querySelectorAll('.pinned-experience').forEach(sec=>ScrollTrigger.create({trigger:sec.querySelector('aside'),start:'top 128px',endTrigger:sec,end:'bottom bottom',pin:true,pinSpacing:false}));}}else{document.querySelectorAll('.reveal,.image-mask').forEach(el=>{el.style.opacity=1;el.style.transform='none'});}
document.querySelectorAll('.magnetic').forEach(el=>{el.addEventListener('mousemove',e=>{const r=el.getBoundingClientRect();el.style.transform=`translate(${(e.clientX-r.left-r.width/2)*.08}px,${(e.clientY-r.top-r.height/2)*.08}px)`});el.addEventListener('mouseleave',()=>el.style.transform='')});
document.querySelectorAll('form[data-static-form]').forEach(f=>f.addEventListener('submit',e=>{e.preventDefault();const n=f.querySelector('.form-note');if(n)n.textContent='This static form is ready, but needs an enquiry endpoint before live submissions.'}));
const lb=document.createElement('div');lb.className='lightbox';lb.innerHTML='<button aria-label="Close">×</button><img alt=""><p></p>';document.body.appendChild(lb);const lbi=lb.querySelector('img'),cap=lb.querySelector('p');document.querySelectorAll('.gallery-item img,.panorama-grid img').forEach(img=>img.addEventListener('click',()=>{lbi.src=img.currentSrc||img.src;lbi.alt=img.alt;cap.textContent=img.alt;lb.classList.add('open')}));lb.addEventListener('click',e=>{if(e.target===lb||e.target.tagName==='BUTTON')lb.classList.remove('open')});addEventListener('keydown',e=>{if(e.key==='Escape')lb.classList.remove('open')});
